import { AuditDetailsFormatPipePipe } from './audit-details-format-pipe.pipe';

describe('AuditDetailsFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AuditDetailsFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
